Current integrations
- None external; runs fully in-browser with local assets.
- Uses static JSON (`pack-data.json`) and HTML pack snippets for shop content.
- No telemetry, backend, or network calls detected (search for fetch/xhr returned none).

Gaps for target
- Need registries and data-driven catalogs similar to SAP Calculator.
- Need animation pipeline and event bus hooks for UI updates.
